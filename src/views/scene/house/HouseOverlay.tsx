import type { SceneViewOverlayProps } from "../types";

const HouseOverlay = ({ overlayId, onClose }: SceneViewOverlayProps) => {
  void overlayId;
  void onClose;
  return null;
};

export default HouseOverlay;
